version: '2'

services:
  opbadge:
    image: ezpaarseproject/ezpaarse-badge:1.0.5
    environment:
      - http_proxy
      - https_proxy
      - OBF_CERT
    volumes:
      - ./ssl:/usr/src/app/app/ssl
    command: npm run conf
